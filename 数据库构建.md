顶级部门的数据库表:
1. 部门id
2. 部门名称
3. 子集部门 id 

二级部门数据库表：
1. 子集部门 id
2. 部门名称


数据库表的归一化，自引用与表自己的归一化。

一种设计思路是只记录直属父级是谁，不记录子部门是谁。

dept_id, parent_id, dept_name, 实际层级。

缺点是在数据库层面无法单词直接查询所有子孙节点，如果不用 Mysql 8.0 的复杂递归 CTE，数据库很难单词抓取整个分支。

思路二： 路径枚举类型，(parent_id + ancestors 祖先路径) -- 企业级主流 


好的上面的这个归一化还没有很好的了解但是通过这个 FIND_IN_SET 这个方法来设计数据库表。

dept_id ，parent_id, parent_ids (来通过顺序的方式来保存父级 id ), dept_name , order_num, leader_id, status,
del_flag(是否被删除的标识), create_time upadte_time.

CREATE DATABASE COMPONEYOA

CREATE TABLE `sys_dept`(
`dept_id` BIGINT(20) NOT NULL AUTO_INCREMENT COMMENT '部门ID主键',
`parent_id` BIGINT(20) NOT NULL DEFAULT 0 COMMENT '父部门ID(顶级部门为0)',
`parent_ids` VARCHAR(255) NOT NULL DEFAULT '' COMMENT '祖级列表(如 0,1,2,3用于全子树查询)',
`dept_name` VARCHAR(50) NOT NULL COMMENT '部门名称',
`leader_id` BIGINT(20) DEFAULT NULL COMMENT '部门负责人用户ID',
`status` TINYINT(1) NOT NULL DEFAULT 0 COMMENT '部门状态(0正常 1停用)',
`del_flag` TINYINT(1) NOT NULL DEFAULT 0 COMMENT '删除标志(0 存在 1 删除)',
`create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
`update_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
PRIMARY KEY(`dept_id`),
KEY `idx_parent_id`(`parent_id`) COMMENT '支撑高效按父级查找直属子级'
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='部门信息表';

ALTER TABLE `sys_dept`
ADD COLUMN `order_num` INT(4) NOT NULL DEFAULT 0 COMMENT '显示顺序' AFTER `dept_name`;



功能模块二： 员工管理模块的设计
员工id
所属部门id
姓名
性别
手机号
用户邮箱
帐号(必须添加唯一约束，数据库唯一索引是防止高并发下重复注册/插入的最后一道防线)
密码
帐号状态
逻辑删除状态
创建时间 (默认添加的)
修改时间 (默认添加的)
创建人 (默认添加的)
修改人 (默认添加的)

CREATE TABLE `sys_user`(
`user_id` BIGINT(20) NOT NULL AUTO_INCREMENT COMMENT '员工/用户ID主键',
`dept_id` BIGINT(20) NOT NULL DEFAULT 0 COMMENT '归属部门ID',
`user_name` VARCHAR(20) NOT NULL COMMENT '登录帐号',
`nick_name` VARCHAR(20) NOT NULL COMMENT '员工姓名',
`password` VARCHAR(100) NOT NULL COMMENT '加密后的密码',
`phone` VARCHAR(11) DEFAULT '' COMMENT '手机号码',
`email` VARCHAR(50) DEFAULT '' COMMENT '用户邮箱',
`sex` CHAR(1) DEFAULT '0' COMMENT '用户性别(0男 1女 2未知)',
`status` TINYINT(1) NOT NULL DEFAULT 0 COMMENT '帐号状态正常(0正常 1停用)',
`del_flag` TINYINT(1) NOT NULL DEFAULT 0 COMMENT '删除标志(0存在 1删除)',
`create_by` VARCHAR(30) DEFAULT '' COMMENT '创始人',
`create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
`update_by` VARCHAR(30) DEFAULT '' COMMENT '修改人',
`update_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
PRIMARY KEY (`user_id`),
UNIQUE KEY `uk_user_name` (`user_name`),
KEY `idx_dept_id` (`dept_id`)
)ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='员工信息表'



功能模块三：
岗位管理模块， 有岗位之后才能够进行流程管理，根据不同的岗位发起流程

岗位id BIGINT(20)
岗位名称 VARCHAR(20)
岗位code VARCHAR(20)
显示排序 INT(4)
状态 TINYINT(1)
逻辑删除 TINYINT(1)
创建时间 DATETIME
创建者 VARCHAR(20)
修改时间 DATETIME
修改人 VARCHAR(20)

CREATE TABLE `sys_post` (
`post_id` BIGINT(20) NOT NULL AUTO_INCREMENT COMMENT '岗位ID(主键)',
`post_code` VARCHAR(64) NOT NULL COMMENT '岗位编码(如 ceo dev_leader)',
`post_name` VARCHAR(20) NOT NULL COMMENT '岗位名称（如 董事长）',
`post_sort` INT(4) NOT NULL DEFAULT 0 COMMENT '显示顺序',
`status` TINYINT(1) NOT NULL DEFAULT 0 COMMENT '状态(0正常 1停用)',
`del_flag` TINYINT(1) NOT NULL DEFAULT 0 COMMENT '删除标志(0 正常 1 删除)',
`create_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
`update_time` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '修改时间',
`create_by` VARCHAR(32) DEFAULT '' COMMENT '创建者',
`update_by` VARCHAR(32) DEFAULT '' COMMENT '修改者',
PRIMARY KEY (`post_id`),
UNIQUE KEY `uk_post_code` (`post_code`)
) ENGINE = InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='岗位信息表';