2026_09_24： 完成对部门的前后端内容的编写，完成最简单的增删改查。如果还有能力的话完善员工模块的数据库创建和功能。

2026_09_25  因为上一次没有完成对部门这个单元的编写，所以现在我要完成对部门这个单元内容的编写，将前后端内容都写好。
1. 首先写好前端的部门管理的页面， 要引入 Element ，页面设计为左侧是 El-tree 的形式右侧是一个列表的形式。
2. 还要在 css 中引入 Sass 模块。

好了前端我直接让 Ai 进行生成的代码，写了左侧的 el-tree 和右侧的搜索与 Table 列表。
学到的内容包括 box-sizing: border-box 的使用方式，因为盒子模型是由 width+padding+border+margin 构成的，当声明了 box-sizing 之后
声明 with:100px 就是 width + padding + border 的宽度了。 
还有一个就是两种写 Vue 单页面的方式一种是适用于 Slot 模板插入的形式即 h 函数渲染的方式，还有一种就是模板的方式，template 的形式。


后端方面：先使用最基本的 Spring 框架进行学习，不要花里胡哨的注解先。
1. 学习到的就是使用引用类型来声明 entity 中的属性， java 中的基本数据类型有: 数值类的: byte short int long 浮点数: float double 字符:char 布尔类型:boolean
2. 在 entity 中进行实际声明的时候 BigInt -> Long tinyInt -> Integer Date-> localDateTime 使用引用类型进行声明的，因为引用类型可以为 null
3. 在写实体类的时候必须要留一个无参构造函数，这样 Spring 框架在使用这个类的时候比如 Mybatits 中塞值，或者 JackSon 传 json 对象 的时候都能通过JVM的反射来拿到这个
Class 中的方法和内容来进行动态的创建，并且通过 Setter 方法进行赋值操作。



2026_09_26 完成对人员模块的 CRUD
【学习总结】：

你的总结系统性非常强，几乎精准覆盖了从“数据库交互 $\to$ 实体映射 $\to$ 业务逻辑 $\to$ Web控制层”的全链路。

不过，从**底层原理和语法严谨性**出发，有 **4 个关键细节**存在小偏差或需要补充，帮你纠偏和补全后，你的这套体系就真正无懈可击了：

---

### 一、 逐条审视与纠偏补全

#### 1. 关于 SQL 拼接空格（总结正确，理解到位）

* **结论**：完全正确。Java 字符串直接 `+` 拼接只是纯字符相加，每一行末尾（或下行开头）主动敲一个空格是防粘连的铁律。

#### 2. 关于 `@Param` 与实体类传值（有 1 处大小写致命细节）

* **修正点**：你在总结中写了 `@Param("UserId") Long userId` 传给 `#{userId}`。
* **底层机制**：**MyBatis 对参数名是严格区分大小写的！** 如果你写 `@Param("UserId")`（大写 U），SQL 里的占位符必须写 `#{UserId}`；如果 SQL 写的是 `#{userId}`（小写 u），运行时会报参数找不到的异常。
* **补充规范**：通常统一使用**小驼峰**命名，即 `@Param("userId") Long userId` 对应 `#{userId}`。

#### 3. 关于实体类的无参构造与 Getter/Setter（原理需要校准）

* **语法纠偏**：构造函数名必须与类名**完全一致（区分大小写）**，类名是 `SysUser`，构造函数就必须是 `public SysUser() {}`，不能小写成 `sysUser`。
* **原理校准**：你总结写了“方便 mapper 进行 Getter 取值映射”。
* 实际上刚好相反：无参构造函数是 MyBatis 从数据库查到数据后，通过反射 `newInstance()` **创建对象**，然后通过 **Setter** 方法把数据库的值塞进对象里！


* **Getter** 方法通常是在执行 `INSERT` 或 `UPDATE` 时，MyBatis 从你的实体类里把属性值“取出来”传给 SQL 占位符。





#### 4. 关于 Service 判断与受影响行数（总结正确）

* **结论**：完全正确。对数据库做写操作（`INSERT` / `UPDATE` / `DELETE`）时，底层返回的都是受影响行数 `int`。在 Service 中用 `rows > 0` 来决定业务成功与否，替代硬编码的 `return false`，是标准的工业级写法。

#### 5. 关于 ID 精度与 Jackson 转换（总结非常到位）

* **结论**：完全正确。后端实体类与 Service/Controller 接收参数时保持强类型的 `Long`，输出给前端时通过 Jackson 全局序列化器转为 `String`，彻底杜绝前端 JS 超过 16 位大数的精度丢失。

#### 6. 关于 Controller 请求动词（依然有一个常见笔误）

* **修正点**：Spring MVC 中**不存在 `@UpdateMapping**`！
* **标准动词四剑客**：
* 新增：`@PostMapping`
* 删除：`@DeleteMapping`
* 修改：**`@PutMapping`**（对应 HTTP PUT）
* 查询：`@GetMapping`


* **泛型类规范**：类名保持大写首字母，如 `Result.success(List<SysUser>)`。

#### 7. 关于 Controller 与 Service 的职责边界（补充 1 个核心要点）

* **总结正确**：Controller 只负责 HTTP 协议层面的事情（接收参数、调用业务、包装统一响应结构 `Result`），业务校验与数据组装下沉到 Service。
* **补充要点（入参注解）**：
* 前端通过 POST / PUT 发送 JSON 请求体时，Controller 方法入参必须加 **`@RequestBody`**（如 `@RequestBody SysUser sysUser`），框架才会调用 Jackson 把 JSON 反序列化为 Java 对象；


* 如果是 URL 上的简单参数（如 `?deptId=4`），才用 `@RequestParam`。



---

### 二、 你的极简脑图复盘卡

```text
[HTTP 请求]
   │
   ├─ URL参数 / QueryString  ──>  @RequestParam Long deptId
   ├─ JSON 请求体 (POST/PUT) ──>  @RequestBody SysUser sysUser
   ▼
[Controller 层]
   ├─ 动词规范：@PostMapping(增), @DeleteMapping(删), @PutMapping(改), @GetMapping(查)
   ├─ 职责边界：只做协议分发与参数接发，不做厚重业务逻辑
   ▼
[Service 层]
   ├─ 业务校验：非空检查、唯一性排重 (selectByUserName)、密码单向加盐 (BCrypt)
   ├─ 状态判定：根据持久层受影响行数 (rows > 0) 返回布尔值或抛出业务异常
   ▼
[Mapper / DB 层]
   ├─ SQL 编写：多行拼接末尾留空格；更新字段时务必在 SET 中包含所有目标字段
   ├─ 参数映射：@Param("key") 严格大小写对应 #{key}；单实体入参直接通过属性名匹配
   ▼
[Entity 规范]
   ├─ 驼峰命名，属性类型用包装类 (Long, Integer, LocalDateTime) 对应 DB 类型
   └─ 显式保留 public SysUser() {} 无参构造，支撑 MyBatis 与 Jackson 的 JVM 反射

```

带上这份总结和修正，你在员工管理这个模块的基础认知就彻底通透了，完全具备了独立搭建企业级 CRUD 核心链路的能力！